/**
 * Configuración DEV — Prometeo Aprobología
 */
var CONFIG = {
  ENV: 'DEV',
  BOT_NAME: 'Agente IDM · Prometeo (DEV)',
  // En DEV el orquestador es simulado (aleatorio).
  USE_MOCK_ORCHESTRATOR: true,
  ORCHESTRATOR_URL: ''
};
